apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: kubernetes-dashboard
    newName: ghcr.io/dlddu/kubernetes-dashboard
    newTag: latest
