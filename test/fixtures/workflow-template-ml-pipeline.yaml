apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: ml-pipeline
  namespace: dashboard-test
spec:
  templates:
    - name: main
      container:
        image: busybox
        command: [echo, "training"]
