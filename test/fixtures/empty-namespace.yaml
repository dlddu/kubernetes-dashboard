apiVersion: v1
kind: Namespace
metadata:
  name: dashboard-empty
  labels:
    app: kubernetes-dashboard
    env: test
    purpose: e2e-testing
  annotations:
    description: "Empty namespace for E2E testing — no Argo resources deployed here"
